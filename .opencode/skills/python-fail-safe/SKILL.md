# Fail-Safe Policy

If the agent encounters ANY uncertainty:

The agent must:

- stop execution
- avoid guessing
- avoid partial solutions
- ask the user for clarification

Never push unstable or speculative code.

Production safety takes priority over speed.